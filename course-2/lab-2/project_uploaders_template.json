{
  "name": "${PROJECT_KEY}_uploaders_pt",
  "resources": {
    "artifact": {
      "actions": {
        "users": {},
        "groups": {
          "${PROJECT_KEY}_uploaders": [
            "WRITE",
            "DELETE",
            "READ",
            "ANNOTATE"
          ]
        }
      },
      "targets": {
        "${PROJECT_KEY}-gradle-rc-local": {
          "include_patterns": [
            "**"
          ],
          "exclude_patterns": []
        },
        "${PROJECT_KEY}-gradle-dev-local": {
          "include_patterns": [
            "**"
          ],
          "exclude_patterns": []
        },
        "${PROJECT_KEY}-gradle-release-local": {
          "include_patterns": [
            "**"
          ],
          "exclude_patterns": []
        },
        "${PROJECT_KEY}-gradle-remote": {
          "include_patterns": [
            "**"
          ],
          "exclude_patterns": []
        },
        "${PROJECT_KEY}-gradle-prod-local": {
          "include_patterns": [
            "**"
          ],
          "exclude_patterns": []
        }
      }
    },
    "build": {
      "actions": {
        "users": {},
        "groups": {}
      },
      "targets": {
        "artifactory-build-info": {
          "include_patterns": [
            "${PROJECT_KEY}-gradle-oidc/**",
            "${PROJECT_KEY}-gradle-app/**"
          ],
          "exclude_patterns": []
        },
        "${PROJECT_KEY}-build-info": {
          "include_patterns": [
            "${PROJECT_KEY}-gradle-oidc/**",
            "${PROJECT_KEY}-gradle-app/**"
          ],
          "exclude_patterns": []
        }
      }
    }
  }
}